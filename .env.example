# Copy to .env (gitignored). Used by scripts/calibration-test.sh only —
# the app itself reads nothing from here.
SUPABASE_URL=https://ymbfqlrarlnqtzatgfah.supabase.co
SUPABASE_ANON_KEY=sb_publishable_...
# A dedicated calibration account. Add its user id to the Supabase project
# secret AI_LIMIT_EXEMPT_USER_IDS so batches clear the daily AI budget.
CALIB_EMAIL=calibration@example.com
CALIB_PASSWORD=change-me
